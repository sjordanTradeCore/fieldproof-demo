# FieldProof Demo

FieldProof OS v1.5 live demo deployment for Railway.

This repository contains a packaged snapshot of the FieldProof full-stack source plus a Railway Dockerfile. The deployed service serves the Expo web app and FastAPI API from the same origin and uses Railway Postgres for persistent demo data.
